---
title: Drawer
description: A panel that slides in from the edge of the screen.
links:
  doc: https://base-ui.com/react/components/drawer
  api: https://base-ui.com/react/components/drawer#api-reference
status: new
---

<ComponentPreview name="drawer-demo" />

## Installation

<CodeTabs>
  <TabsList>
    <TabsTab value="cli">CLI</TabsTab>
    <TabsTab value="manual">Manual</TabsTab>
  </TabsList>

  <TabsPanel value="cli">
    ```bash
    npx shadcn@latest add @lumi-ui/drawer
    ```
  </TabsPanel>

  <TabsPanel value="manual">
    <Steps>
      <Step title="Copy and paste the following code into your project.">
        <ComponentSourceCode name="drawer" title="drawer.tsx" collapsible/>
      </Step>
    </Steps>
  </TabsPanel>
</CodeTabs>

Add the following utilities to your `globals.css`:

```css title="globals.css"
@layer utilities {
  .drawer-popup {
    touch-action: auto;
    transition: transform 450ms var(--ease-smooth);

    &[data-swiping] {
      @apply select-none;
    }

    &[data-ending-style] {
      transition-duration: calc(var(--drawer-swipe-strength) * 400ms);
    }
  }

  /* Bottom */
  .drawer-popup[data-side="bottom"] {
    @apply -mb-(--drawer-bleed);
    max-height: calc(80vh + var(--drawer-bleed));
    transform: translateY(var(--drawer-swipe-movement-y));

    &[data-ending-style],
    &[data-starting-style] {
      transform: translateY(calc(100% - var(--drawer-bleed) + 2px));
    }
  }

  /* Top */
  .drawer-popup[data-side="top"] {
    @apply -mt-(--drawer-bleed);
    max-height: calc(80vh + var(--drawer-bleed));
    transform: translateY(var(--drawer-swipe-movement-y));

    &[data-ending-style],
    &[data-starting-style] {
      transform: translateY(calc(-100% + var(--drawer-bleed) - 2px));
    }
  }

  /* Left */
  .drawer-popup[data-side="left"] {
    @apply -ml-(--drawer-bleed) h-full w-[calc(20rem+var(--drawer-bleed))] sm:w-[calc(24rem+var(--drawer-bleed))] max-w-[100vw];
    --viewport-padding: 0px;
    transform: translateX(var(--drawer-swipe-movement-x));

    &[data-ending-style],
    &[data-starting-style] {
      transform: translateX(calc(-100% + var(--drawer-bleed) - var(--viewport-padding) - 2px));
    }

    @supports (-webkit-touch-callout: none) {
      --drawer-bleed: 0px;
      --viewport-padding: 0.625rem;
      @apply ml-0 w-80 sm:w-96 max-w-[calc(100vw-20px)] rounded-md pl-6;
    }
  }

  /* Right */
  .drawer-popup[data-side="right"] {
    @apply -mr-(--drawer-bleed) h-full w-[calc(20rem+var(--drawer-bleed))] sm:w-[calc(24rem+var(--drawer-bleed))] max-w-[100vw];
    --viewport-padding: 0px;
    transform: translateX(var(--drawer-swipe-movement-x));

    &[data-ending-style],
    &[data-starting-style] {
      transform: translateX(calc(100% - var(--drawer-bleed) + var(--viewport-padding) + 2px));
    }

    @supports (-webkit-touch-callout: none) {
      --drawer-bleed: 0px;
      --viewport-padding: 0.625rem;
      @apply mr-0 w-80 sm:w-96 max-w-[calc(100vw-20px)] rounded-md pr-6;
    }
  }

  /* Viewport side variants */
  .drawer-viewport[data-side="left"],
  .drawer-viewport[data-side="right"] {
    @supports (-webkit-touch-callout: none) {
      --viewport-padding: 0.625rem;
      padding: var(--viewport-padding);
    }
  }
}
```


## Basic Usage
```tsx
import {
  Drawer,
  DrawerClose,
  DrawerDescription,
  DrawerFooter,
  DrawerHeader,
  DrawerContent,
  DrawerTitle,
  DrawerTrigger,
} from "@/components/ui/drawer";

export function DrawerDemo() {
  return (
    <Drawer>
      <DrawerTrigger>Open Drawer</DrawerTrigger>
      <DrawerContent>
        <DrawerHeader>
          <DrawerTitle>Are you absolutely sure?</DrawerTitle>
          <DrawerDescription>
            This action cannot be undone. This will permanently delete your
            account and remove your data from our servers.
          </DrawerDescription>
        </DrawerHeader>
        <DrawerFooter>
          <DialogClose>Cancel</DialogClose>
          <DialogClose>Continue</DialogClose>
        </DrawerFooter>
      </DrawerContent>
    </Drawer>
  );
}
```

## Anatomy

<Tabs defaultValue="common">
  <TabsList>
    <TabsTab value="common">Common usage</TabsTab>
    <TabsTab value="primitive">Primitive usage</TabsTab>
  </TabsList>

  <TabsPanel value="common">
```tsx
<DrawerProvider>
  <DrawerIndentBackground />
  <DrawerIndent>
  <Drawer>
    <DrawerTrigger />
    <DrawerSwipeArea />
    <DrawerContent>
      <DrawerSelectable>
        <DrawerHeader>
          <DrawerTitle />
          <DrawerDescription />
        </DrawerHeader>
        <DrawerFooter>
          <DrawerClose />
        </DrawerFooter>
      </DrawerSelectable>
    </DrawerPortal>
  </Drawer>
  </DrawerIndent>
</DrawerProvider>
```
  </TabsPanel>
  <TabsPanel value="primitive">
```tsx
<DrawerProvider>
  <DrawerIndentBackground />
  <DrawerIndent>
  <Drawer>
    <DrawerTrigger />
    <DrawerSwipeArea />
    <DrawerPortal>
      <DrawerBackdrop />
      <DrawerViewport>
        <DrawerPopup>
          <DrawerSelectable>
            <DrawerHeader>
              <DrawerTitle />
              <DrawerDescription />
            </DrawerHeader>
            <DrawerFooter>
              <DrawerClose />
            </DrawerFooter>
          </DrawerSelectable>
        </DrawerPopup>
      </DrawerViewport>
    </DrawerPortal>
  </Drawer>
  </DrawerIndent>
</DrawerProvider>
```
  </TabsPanel>
</Tabs>

## Directions
<ComponentPreview name="drawer-directions" />

## Cookbook

### Enable content selection inside drawer

<ComponentPreview name="drawer-selection" />

### Scrollable content

By default, the height of drawer is about 80vh. You can control the height of drawer by either setting a fixed height in the scroll container or changing source code.

<ComponentPreview name="drawer-scroll" />

### Stacked drawer

<ComponentPreview name="drawer-stacked" />

To enable stacking, use the `Stacked` variant for `DrawerContent` and add the following CSS:

```css title="globals.css"
@layer utilities {
  .drawer-stacked-popup {
    touch-action: auto;
    @apply -mb-(--drawer-bleed);
    max-height: calc(80vh + var(--drawer-bleed));

    /* Stacking math */
    --peek: 1rem;
    --stack-progress: clamp(0, var(--drawer-swipe-progress), 1);
    --stack-step: 0.05;
    --stack-peek-offset: max(0px, calc((var(--nested-drawers) - var(--stack-progress)) * var(--peek)));
    --scale-base: calc(max(0, 1 - (var(--nested-drawers) * var(--stack-step))));
    --scale: clamp(0, calc(var(--scale-base) + (var(--stack-step) * var(--stack-progress))), 1);
    --shrink: calc(1 - var(--scale));
    --height: max(0px, calc(var(--drawer-frontmost-height, var(--drawer-height)) - var(--drawer-bleed)));

    height: var(--drawer-height, auto);
    transform-origin: 50% calc(100% - var(--drawer-bleed));
    transform:
      translateY(calc(var(--drawer-swipe-movement-y) - var(--stack-peek-offset) - (var(--shrink) * var(--height))))
      scale(var(--scale));

    transition:
      transform 450ms var(--ease-smooth),
      height 450ms var(--ease-smooth),
      box-shadow 450ms var(--ease-smooth);

    /* Shadow */
    box-shadow: 0 2px 10px rgb(0 0 0 / 0.1);

    /* Pseudo overlay for nested dimming */
    &::after {
      content: "";
      @apply absolute inset-0 rounded-[inherit] bg-transparent pointer-events-none;
      transition: background-color 450ms var(--ease-smooth);
    }

    /* Enter/exit */
    &[data-ending-style],
    &[data-starting-style] {
      transform: translateY(calc(100% - var(--drawer-bleed) + 2px));
    }

    &[data-ending-style] {
      box-shadow: 0 2px 10px rgb(0 0 0 / 0);
      transition-duration: calc(var(--drawer-swipe-strength) * 400ms);
    }

    /* Swipe states */
    &[data-swiping] {
      transition-duration: 0s;
      @apply select-none;
    }

    &[data-nested-drawer-swiping] {
      transition-duration: 0s;
    }

    /* Nested drawer open */
    &[data-nested-drawer-open] {
      height: calc(var(--height) + var(--drawer-bleed));
      overflow: hidden;

      &::after {
        background-color: rgb(0 0 0 / 0.05);
      }
    }
  }
}
```

### Snap points

<ComponentPreview name="drawer-snap" />

For snap points support, add the following CSS:

```css title="globals.css"
@layer utilities {
  .drawer-snap-popup {
    @apply overflow-visible min-h-0 touch-none;
    max-height: calc(100dvh - var(--drawer-snap-top-margin, 1rem));

    /* Snap-based transform + dynamic padding */
    transform: translateY(calc(var(--drawer-snap-point-offset) + var(--drawer-swipe-movement-y)));
    padding-bottom: max(0px, calc(var(--drawer-snap-point-offset) + var(--drawer-swipe-movement-y)));

    transition:
      transform 450ms var(--ease-smooth);

    /* Bleed pseudo-element below rounded corners */
    &::after {
      content: "";
      @apply pointer-events-none absolute inset-x-0 top-full;
      height: var(--drawer-bleed);
      background-color: inherit;
    }

    &[data-swiping] {
      @apply select-none;
    }

    &[data-ending-style],
    &[data-starting-style] {
      transform: translateY(calc(100% + 2px));
      padding-bottom: 0;
      box-shadow:
        0 -16px 48px rgb(0 0 0 / 0),
        0 6px 18px rgb(0 0 0 / 0);
    }

    &[data-ending-style] {
      transition-duration: calc(var(--drawer-swipe-strength) * 400ms);
    }
  }
}
```

### Drawer Indent

Visit [Playground](https://www.lumiui.dev/playground/drawer-indent) to try it out.

#### Setup

```tsx
import {
  DrawerIndent,
  DrawerIndentBackground,
  DrawerProvider,
} from "@/components/ui/drawer";

export default function DrawerIndentPage() {
  return (
    <DrawerProvider>
      <DrawerIndentBackground />
      <DrawerIndent>
        {/* Your drawer */}
      </DrawerIndent>
    </DrawerProvider>
  );
}
```

More info at [Base UI](https://base-ui.com/react/components/drawer#indent-effect).

#### CSS

```css title="globals.css"
@layer utilities {
  .drawer-indent {
    --indent-progress: var(--drawer-swipe-progress);
    --indent-radius: calc(1rem * (1 - var(--indent-progress)));
    --indent-transition: calc(1 - clamp(0, calc(var(--drawer-swipe-progress) * 100000), 1));

    @apply origin-[center_top] will-change-transform;
    transform: scale(1) translateY(0);
    transition:
      transform 0.4s var(--ease-smooth),
      border-radius 0.25s var(--ease-smooth);
    transition-duration:
      calc(400ms * var(--indent-transition)),
      calc(250ms * var(--indent-transition));

    &[data-active] {
      transform:
        scale(calc(0.98 + (0.02 * var(--indent-progress))))
        translateY(calc(0.5rem * (1 - var(--indent-progress))));
      border-top-left-radius: var(--indent-radius);
      border-top-right-radius: var(--indent-radius);
    }
  }
}
```

### Customization

#### Mobile navigation

<ComponentPreview name="drawer-mobile-navigation" />

#### Action sheet

<ComponentPreview name="drawer-action-sheet" />

#### Responsive

<ComponentPreview name="drawer-responsive" />