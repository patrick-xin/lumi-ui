---
title: Accordion
description: A set of collapsible panels with headings.
links:
  doc: https://base-ui.com/react/components/accordion
  api: https://base-ui.com/react/components/accordion#api-reference
---

<ComponentPreview name="accordion-demo" />

## Installation

<CodeTabs>
  <TabsList>
    <TabsTab value="cli">CLI</TabsTab>
    <TabsTab value="manual">Manual</TabsTab>
  </TabsList>

  <TabsPanel value="cli">
    ```bash
    npx shadcn@latest add @lumi-ui/accordion
    ```
  </TabsPanel>

  <TabsPanel value="manual">
    <Steps>
      <Step title="Copy and paste the following code into your project.">
        <ComponentSourceCode name="accordion" title="accordion.tsx" collapsible/>
      </Step>
      <Step title="Update the import paths to match your project setup."/>
    </Steps>
  </TabsPanel>
</CodeTabs>

## Basic Usage

```tsx
import {
  Accordion,
  AccordionItem,
  AccordionPanel,
  AccordionSummary,
} from "@/components/ui/accordion";
```

```tsx
export function AccordionDemo() {
  return (
    <Accordion defaultValue={["item-1"]} className="w-full max-w-md">
      <AccordionItem value="item-1">
        <AccordionSummary>What is Lumi UI?</AccordionSummary>
        <AccordionPanel>
          Lumi UI is an open-source component library designed for building
          beautiful and accessible user interfaces with React.
        </AccordionPanel>
      </AccordionItem>
      <AccordionItem value="item-2">
        <AccordionSummary>Can I customize the components?</AccordionSummary>
        <AccordionPanel>
          Absolutely. Components are built with Tailwind CSS and CVA, making
          them easy to style and extend to match your brand.
        </AccordionPanel>
      </AccordionItem>
    </Accordion>
  );
}
```

## Anatomy

<Tabs defaultValue="Common">
  <TabsList>
    <TabsTab value="Common">Common usage</TabsTab>
    <TabsTab value="Primitive">Primitive usage</TabsTab>
  </TabsList>

  <TabsPanel value="Common">
```tsx
<Accordion>
  <AccordionItem>
    <AccordionSummary/>
    <AccordionPanel/>
  </AccordionItem>
</Accordion>
```
  </TabsPanel>

  <TabsPanel value="Primitive">
```tsx
<Accordion>
  <AccordionItem>
    <AccordionHeader>
      <AccordionTrigger/>
    </AccordionHeader>
    <AccordionPanel/>
  </AccordionItem>
</Accordion>
```
  </TabsPanel>
</Tabs>

## Cookbook

### Custom Trigger with icon

<ComponentPreview name="accordion-custom-trigger-icon" />

### Custom Trigger without icon

<ComponentPreview name="accordion-custom-trigger-without-icon" />

### Default Open Panels

To have an accordion item open by default, assign a unique value to the `AccordionItem` and pass that value in an array to the `defaultValue` prop on `Accordion` component.

<ComponentPreview name="accordion-default-open-single" />

To have multiple items open by default, ensure the `multiple` prop is set to `true` and include each item's value in the `defaultValue` array.

<ComponentPreview name="accordion-default-open-multiple" />

### Open multiple panels

Set up the accordion to allow multiple panels to be open at the same time using the `multiple` prop.

<ComponentPreview name="accordion-single" />

### Disable Panel

Pass `disabled` props to `AccordionItem` to disable a panel.

<ComponentPreview name="accordion-disabled" />

### Usage with Motion

Base UI spreads its `className` (including transition styles) onto 
your custom component via the `render` prop. This causes both CSS and JS 
animations to run simultaneously, resulting in janky behavior.

<Callout variant="info" title="Important">
  When using `render` with animation libraries like Motion, you can set 
  `animation="none"` to reset default css transitions and prevent conflicts between CSS transitions and JavaScript animations.
</Callout>

<ComponentPreview name="accordion-motion" />


## API Reference

| Component | Description
| :--- | :--- |
| [`Accordion`](https://base-ui.com/react/components/accordion#accordion)  | Groups all parts of the accordion. Renders a `<div>` element.
| [`AccordionItem`](https://base-ui.com/react/components/accordion#accordionitem)  | Groups an accordion header with the corresponding panel. Renders a `<div>` element.
| [`AccordionHeader`](https://base-ui.com/react/components/accordion#accordionheader)  | A heading that labels the corresponding panel. Renders an `<h3>` element.
| [`AccordionTrigger`](https://base-ui.com/react/components/accordion#accordiontrigger)  | A button that opens and closes the corresponding panel. Renders a `<button>` element.
| [`AccordionPanel`](https://base-ui.com/react/components/accordion#accordionpanel)  | A collapsible panel with the accordion item contents. Renders a `<div>` element.
| `AccordionSummary` | Styled container that wraps the `AccordionHeader` and `AccordionTrigger`.

### AccordionPanel

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `animation` | `"css"` \| `"none"` | `"css"` | Controls default animation of the accordion panel. |



