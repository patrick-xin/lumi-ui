---
title: Alert Dialog
description: A modal dialog that interrupts the user with important content and expects a response.
links:
  doc: https://base-ui.com/react/components/alert-dialog
  api: https://base-ui.com/react/components/alert-dialog#api-reference
---

<ComponentPreview name="alert-dialog-demo" />

## Installation

<CodeTabs>
  <TabsList>
    <TabsTab value="cli">CLI</TabsTab>
    <TabsTab value="manual">Manual</TabsTab>
  </TabsList>

  <TabsPanel value="cli">
    ```bash
    npx shadcn@latest add @lumi-ui/alert-dialog
    ```
  </TabsPanel>

  <TabsPanel value="manual">
    <Steps>
      <Step title="Copy and paste the following code into your project.">
        <ComponentSourceCode name="alert-dialog" title="alert-dialog.tsx" collapsible/>
      </Step>
    </Steps>
  </TabsPanel>
</CodeTabs>

## Basic Usage

```tsx
import {
  AlertDialog,
  AlertDialogClose,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogContent,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/components/ui/alert-dialog";
```
```tsx
export function AlertDialogDemo() {
  return (
    <AlertDialog>
      <AlertDialogTrigger>Show Alert Dialog</AlertDialogTrigger>
      <AlertDialogContent>
        <AlertDialogHeader>
          <AlertDialogTitle>Are you absolutely sure?</AlertDialogTitle>
          <AlertDialogDescription>
            This action cannot be undone. This will permanently delete your
            account and remove your data from our servers.
          </AlertDialogDescription>
        </AlertDialogHeader>
        <AlertDialogFooter>
          <AlertDialogClose>Cancel</AlertDialogClose>
          <AlertDialogClose>Continue</AlertDialogClose>
        </AlertDialogFooter>
      </AlertDialogContent>
    </AlertDialog>
  );
}
```

## Anatomy

<Tabs defaultValue="common">
  <TabsList>
    <TabsTab value="common">Common usage</TabsTab>
    <TabsTab value="primitive">Primitive usage</TabsTab>
  </TabsList>

  <TabsPanel value="common">
    ```tsx
    <AlertDialog>
      <AlertDialogTrigger />
      <AlertDialogContent>
        <AlertDialogHeader>
          <AlertDialogTitle />
          <AlertDialogDescription />
        </AlertDialogHeader>
        <AlertDialogFooter>
          <AlertDialogClose />
        </AlertDialogFooter>
      </AlertDialogContent>
    </AlertDialog>
  ```
  </TabsPanel>

  <TabsPanel value="primitive">
  ```tsx
  <AlertDialog>   
    <AlertDialogTrigger />
    <AlertDialogPortal>     
      <AlertDialogBackdrop />     
      <AlertDialogViewport>       
        <AlertDialogPopup>         
          <AlertDialogTitle />         
          <AlertDialogDescription />         
          <AlertDialogClose />       
        </AlertDialogPopup>     
      </AlertDialogViewport>   
    </AlertDialogPortal> 
  </AlertDialog>
  ```
  </TabsPanel>
</Tabs>


## Cookbook

### Open from dropdown menu

Control the dialog state and open it imperatively using the `onClick` handler on the `DropdownMenuItem`.

<ComponentPreview name="alert-dialog-open-from-menu" />

### Close confirmation

This example shows a nested confirmation dialog that opens if the text entered in the parent dialog is going to be discarded.

To implement this, both dialogs should be controlled. The confirmation dialog may be opened when `onOpenChange` callback of the parent dialog receives a request to close. This way, the confirmation is automatically shown when the user clicks the backdrop, presses the Esc key, or clicks a close button.

<ComponentPreview name="alert-dialog-close-confirmation" />

### Detached triggers

An alert dialog can be controlled by a trigger located either inside or outside the `<AlertDialog>` component. For simple, one-off interactions, place the `<AlertDialogTrigger>` inside `<AlertDialog>`, as shown in the demo example.

However, if defining the alert dialog’s content next to its trigger is not practical, you can use a detached trigger. This involves placing the `<AlertDialogTrigger>` outside of `<AlertDialog>` and linking them with a handle created by the `createAlertDialogHandle` function.

```tsx
const deleteAlertHandle = createAlertDialogHandle();
```

<ComponentPreview name="alert-dialog-detached-triggers" />

### Multiple triggers

A single alert dialog can be opened by multiple trigger elements. You can achieve this by using the same handle for several detached triggers, or by placing multiple `<AlertDialogTrigger>` components inside a single `<AlertDialog>`.

```tsx title="Multiple triggers within the Root part"
<AlertDialogRoot>
  <AlertDialogTrigger>Trigger 1</AlertDialogTrigger>
  <AlertDialogTrigger>Trigger 2</AlertDialogTrigger>
  ...
</AlertDialogRoot>
```
```tsx title="Multiple detached triggers"
const demoAlertDialog = createAlertDialogHandle();

<AlertDialogTrigger handle={demoAlertDialog}>Trigger 1</AlertDialogTrigger>
<AlertDialogTrigger handle={demoAlertDialog}>Trigger 2</AlertDialogTrigger>
<AlertDialog handle={demoAlertDialog}>
  ...
</AlertDialog>
```

The alert dialog can also render different content depending on which trigger opened it. This is achieved by passing a payload to the `<AlertDialogTrigger>` and using the function-as-a-child pattern in `<AlertDialog>`.

The payload can be strongly typed by providing a type argument to the `createAlertDialogHandle()` function.

<ComponentPreview name="alert-dialog-render-different-content" />

### Controlled mode with multiple triggers

You can control the alert dialog’s open state externally using the `open` and `onOpenChange` props on `<AlertDialog>`. This allows you to manage the alert dialog’s visibility based on your application’s state. When using multiple triggers, you have to manage which trigger is active with the `triggerId` prop on `<AlertDialog>` and the `id` prop on each `<AlertDialogTrigger>`.

Note that there is no separate onTriggerIdChange prop. Instead, the `onOpenChange` callback receives an additional argument, `eventDetails`, which contains the trigger element that initiated the state change.

<ComponentPreview name="alert-dialog-controlled-mode" />

## Create your own

Most of the time, you can use `AlertDialogContent` directly, which wraps `AlertDialogPortal`, `AlertDialogViewport`, `AlertDialogBackdrop` and `AlertDialogPopup`. However, you can also customize your own by using the primitives to give a unique look that fits your design, all styles can be overwritten.

<Callout title="Note" variant="warning">
  `AlertDialogPopup` requires `AlertDialogPortal` to be its parent.
</Callout>

<ComponentPreview name="alert-dialog-custom" />


## API Reference

| Component | Description |
| :--- | :--- |
| [`AlertDialog`](https://base-ui.com/react/components/alert-dialog#root) | Groups all parts of the alert dialog. Doesn’t render its own HTML element. |
| [`AlertDialogTrigger`](https://base-ui.com/react/components/alert-dialog#trigger) | A button that opens the alert dialog. |
| [`AlertDialogPortal`](https://base-ui.com/react/components/alert-dialog#portal) | A portal element that moves the popup to a different part of the DOM. |
| [`AlertDialogViewport`](https://base-ui.com/react/components/alert-dialog#viewport) | A positioning container for the dialog popup that can be made scrollable. |
| [`AlertDialogPopup`](https://base-ui.com/react/components/alert-dialog#popup) | A container for the alert dialog contents. Must be used inside `AlertDialogPortal`. |
| [`AlertDialogBackdrop`](https://base-ui.com/react/components/alert-dialog#backdrop) | An overlay displayed beneath the popup.  |
| `AlertDialogContent` | Pre assembled component that wraps `AlertDialogPortal`, `AlertDialogBackdrop` and `AlertDialogPopup` with default styles. |
| `AlertDialogHeader` | Styled container for convenience purpose. |
| `AlertDialogFooter` | Styled container for convenience purpose. |
| [`AlertDialogTitle`](https://base-ui.com/react/components/alert-dialog#title) | A heading that labels the dialog.  |
| [`AlertDialogDescription`](https://base-ui.com/react/components/alert-dialog#description) | A paragraph with additional information about the alert dialog.  |
| [`AlertDialogClose`](https://base-ui.com/react/components/alert-dialog#close) | A button that closes the alert dialog. |